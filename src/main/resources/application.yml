spring:
  config:
    import : security-variables.yml                          // 보안 파일 import
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url : jdbc:mysql://localhost:3306/dsgram_ds04011
    username: root
    password: ${mysql.password}
  thymeleaf:
    cache: false
  application:
    name: dsgram
mybatis:
  mapper-locations: mappers/*Mapper.xml
